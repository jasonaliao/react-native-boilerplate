import { StyleSheet } from 'react-native';
import { COLOR_PRIMARY, COLOR_SECONDARY} from './common';

export default StyleSheet.create({
    container: {
        flex: 1
    },
    h1: {
        fontSize: 20,
        color: COLOR_PRIMARY,
    },

});